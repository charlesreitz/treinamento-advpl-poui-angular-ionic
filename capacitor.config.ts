import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'treinamento-advpl-poui-angular-ionic',
  webDir: 'www'
};

export default config;
